# Assets embedded by bundle.sh so the apps open straight from disk (file://):
# the Raku.js WebAssembly build (base64) and the CLI showcase sources the apps
# reuse. A few MB; regenerate by re-running bundle.sh.
lib/
